<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="itson.sushivan" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SushiVanApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".BienvenidaActivity"
            android:label="@string/title_activity_bienvenida"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PorGrupoActivity" >
        </activity>
        <activity android:name=".PerfilActivity" >
        </activity>
        <activity android:name=".InfoProductoActivity" >
        </activity>
        <activity android:name=".CarritoActivity" >
        </activity>

        <service
            android:name=".CambioPedidoService"
            android:exported="false" >
        </service>
    </application>

</manifest>
